enum HomeSideMenuOptions {
  buy,
  sell,
  repositroyManagment,
  financial,
  reports,
  setting,
}

enum discountTypes { product, invoice }

enum InvoiceDiscountType {
  minimalPercentage,
  minimalValue,
  alternatingValue,
  alternatingPercentage
}

enum InvoicePayOption { cash, havale, check }
