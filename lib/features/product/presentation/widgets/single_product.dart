import 'package:flutter/material.dart';

class SingleProductListItem extends StatelessWidget {
  const SingleProductListItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container(
        color: Colors.red,
      );
}
